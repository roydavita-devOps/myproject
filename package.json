{
  "name": "umkm-website-builder",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "seed": "npm --prefix backend run seed",
    "smoke-test": "playwright test --config=playwright.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
